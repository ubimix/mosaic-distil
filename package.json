{
  "name": "mosaic-distil",
  "version": "0.1.1",
  "scripts": {
    "bump": "webpack && npm version patch && git push && git push --tags && npm publish",
    "bump-minor": "npm run bump && npm version minor && git push && git push --tags && npm publish",
    "commit": "git commit -a"
  },  
  "dependencies": {
    "adm-zip": "^0.4.7",
    "cheerio": "^0.18.0",
    "elasticsearch": "^3.1.3",
    "jsdom": "^6.5.1",
    "mosaic-commons": "0.0.15",
    "pg.js": "^3.6.2",
    "shapefile": "^0.3.0",
    "superagent": "^0.20.0",
    "terraformer-wkt-parser": "^1.0.1",
    "togeojson": "^0.8.0",
    "underscore": "^1.8.3"
  },
  "engines": {
    "node": "latest",
    "npm": "latest"
  }
}
